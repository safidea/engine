import React from 'react'
import { RowUI } from '@entities/app/page/component/row/RowComponentUI'

const RowUnstyledUI: RowUI = {
  Row: ({ children }) => <div>{children}</div>,
}

export default RowUnstyledUI
