{
  "name": "foundation",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "scripts/start.sh -m prod",
    "dev": "scripts/start.sh -m dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "build": "turbo run build",
    "schema": "turbo run schema",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,md,json}\"",
    "precommit": "pnpm schema && pnpm format && pnpm lint && git add .",
    "prepare": "husky install"
  },
  "devDependencies": {
    "eslint": "8.37.0",
    "eslint-config-foundation": "workspace:*",
    "husky": "8.0.3",
    "prettier": "latest",
    "turbo": "^1.10.5"
  },
  "engines": {
    "node": ">=18.16.0",
    "pnpm": ">=7.30.0"
  },
  "packageManager": "pnpm@7.30.0"
}
