{
  "root": true,
  "extends": ["foundation"],
  "overrides": [
    {
      "files": ["test/**/*.ts"],
      "rules": {
        // Allow the usage of `any`
        "@typescript-eslint/no-explicit-any": "off"
      }
    }
  ]
}
