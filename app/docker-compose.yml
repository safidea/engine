version: '3.9'

networks:
  systems-network:
    driver: bridge

services:
  db:
    image: postgres:13
    container_name: systems-tests-db
    ports:
      - '5434:5432'
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: master
    volumes:
      - ./data:/var/lib/postgresql/data
    networks:
      - systems-network